# Knihovna pro přehrávání zvuku v headless režimu
# (Na Linuxu vyžaduje také: sudo apt install libsdl2-mixer-2.0-0)
pygame-ce>=2.5.0

# Knihovna pro přímé čtení z hardwarové klávesnice v Linuxu
# (Funguje pouze na Linux/Ubuntu/Raspberry Pi, vyžaduje práva root/sudo)
evdev>=1.7.0; sys_platform == 'linux'

pynput>=1.7.6; sys_platform == 'win32'

# Knihovna pro čtení konfiguračního souboru config.yaml
pyyaml>=6.0.1

yeelight>=0.7.14